import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// Or import CurrencyConvertor if rendering separately

ReactDOM.render(<App />, document.getElementById('root'));